{
  "name": "@ai-ec/db",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "vitest run --passWithNoTests",
    "generate": "drizzle-kit generate --config drizzle.config.ts"
  },
  "dependencies": {
    "@ai-ec/core": "workspace:*",
    "@aws-sdk/client-rds-data": "^3.687.0",
    "drizzle-orm": "^0.36.1",
    "zod": "^3.23.8"
  },
  "devDependencies": {
    "drizzle-kit": "^0.28.0",
    "typescript": "^5.6.3",
    "vitest": "^2.1.4"
  }
}
